import van from './node_modules/vanjs-core/src/van.js';

// Try to reproduce the way the active state works and why the button isn't re-rendering its class
console.log("If a class uses an arrow function, does VanJS re-evaluate it automatically without reactive components inside the element?");
